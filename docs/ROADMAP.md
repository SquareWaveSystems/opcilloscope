# opcilloscope Roadmap

Planned features and improvements.

---

## Command Line Usage

Full CLI support for scripting and automation:

```bash
# Launch with a saved configuration
opcilloscope config.cfg
opcilloscope --config production.json

# Show help
opcilloscope --help
```

## Remote Monitoring

Run on headless servers without GUI dependencies — monitor OPC UA data remotely via SSH or lightweight terminal connections.
